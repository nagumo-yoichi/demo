- hosts: all
  become: true

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600
      become: true
      environment:
        PATH: "{{ ansible_env.PATH }}:/usr/bin"
